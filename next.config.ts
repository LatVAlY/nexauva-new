import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['framerusercontent.com', 'x.com', 'images.unsplash.com', 'media.licdn.com'],
    
  },
};

export default nextConfig;
